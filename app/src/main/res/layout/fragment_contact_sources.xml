<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
                name="viewModel"
                type="com.r4sh33d.duplicatecontactsremover.contactsources.ContactSourcesViewModel"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <TextView
                app:contactAccountsList="@{viewModel.contactsAccountList}"
                android:textColor="@android:color/black"
                android:text="Please select a contact account to remove duplicates"
                android:gravity="center"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" android:id="@+id/header_message"
                app:layout_constraintTop_toTopOf="parent" app:layout_constraintEnd_toEndOf="parent"
                android:layout_marginEnd="8dp" app:layout_constraintStart_toStartOf="parent"
                android:layout_marginStart="8dp" android:layout_marginTop="20dp"/>
        <androidx.recyclerview.widget.RecyclerView
                app:contactAccountsList="@{viewModel.contactsAccountList}"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                android:layout_width="0dp"
                android:layout_height="0dp"
                app:layout_constraintStart_toStartOf="parent" android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp" app:layout_constraintEnd_toEndOf="parent"
                android:layout_marginBottom="16dp"
                app:layout_constraintBottom_toBottomOf="parent" android:layout_marginTop="20dp"
                app:layout_constraintTop_toBottomOf="@+id/header_message" android:id="@+id/contactSourcesRecyclerView"/>

        <ProgressBar
                app:accountsLoadingStatus="@{viewModel.status}"
                android:id="@+id/progressBar"
                style="?android:attr/progressBarStyleLarge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginTop="16dp"
                android:layout_marginEnd="16dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintHorizontal_bias="0.5"
                app:layout_constraintBottom_toTopOf="@+id/progressMessage"
                app:layout_constraintVertical_chainStyle="packed"/>

        <TextView
                app:accountsLoadingStatus="@{viewModel.status}"
                android:id="@+id/progressMessage"
                android:layout_width="166dp"
                android:layout_height="26dp"
                android:layout_marginEnd="16dp"
                android:fontFamily="sans-serif-normal"
                android:gravity="center"
                android:textColor="?colorAccent"
                android:textSize="18sp"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="Loading, Please wait" app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/progressBar"
                android:layout_marginTop="16dp" android:layout_marginStart="16dp"
                app:layout_constraintStart_toStartOf="parent" android:layout_marginBottom="16dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
